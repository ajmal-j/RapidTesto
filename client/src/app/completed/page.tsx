import Wrapper from "@/components/layout/Wrapper";
import React from "react";

type Props = {};

const page = ({}: Props) => {
  return (
    <Wrapper>
      <div>Completed</div>
    </Wrapper>
  );
};

export default page;
